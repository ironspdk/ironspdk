# ironspdk-sys

Auxiliary to `ironspdk` crate.

Low-level C FFI bindings to SPDK.

Building SPDK apps has non-trivial caveats, that's why this crate exists.

See README.md of `ironspdk` crate for details.
